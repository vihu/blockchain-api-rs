### blockchain-api-rs

Rust rest api for helium blockchain backed by sqlx + tide.

### Steps to run:

Move `.envsample` to `.env` and edit accordingly.

```
cargo build
cargo run
```

### Supported routes:

```
/api/blocks
```

### Disclaimer:

Expect dragons
